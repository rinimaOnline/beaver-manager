VITE_API_ENV=prod
# 同源：OpenResty 的管理端站点已把 /admin/ 反代到 gateway_admin(40800)、
# /api/ 反代到 gateway(20800)。两个都留空就不会跨域，换域名也不用重新打包。
#
# 这里**绝不能填第三方地址**——之前写的是上游作者的 server.wsrh8888.com，
# 那样打出来的后台会把管理请求（含登录凭据）发到别人服务器上。
VITE_API_BASE=''
VITE_API_API=''
